<Window x:Class="PcSborka.Views.Windows.InputEmailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PcSborka.Views.Windows"
        mc:Ignorable="d"
        Title="Введите свою почту" Height="150" Width="320" MinHeight="125" MinWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <TextBlock VerticalAlignment="Center" TextAlignment="Right"  FontSize="14" Margin="5" FontStyle="Italic" FontWeight="Bold">Почта:</TextBlock>
        <TextBox  x:Name="Email_textBox" VerticalAlignment="Center"  FontSize="14" Margin="5" FontStyle="Italic" FontWeight="Bold" Grid.Column="1" Cursor="Arrow"></TextBox>
        <Button x:Name="NextWindow_button" VerticalAlignment="Bottom"  HorizontalAlignment="Center" FontSize="14" Margin="5" FontStyle="Italic" FontWeight="Bold" Grid.Row="1" Grid.ColumnSpan="2" Click="NextWindow_button_Click">Посмореть заказы</Button>
    </Grid>
</Window>
